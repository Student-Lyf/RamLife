import "package:flutter/material.dart";

class Footer extends StatelessWidget {
	@override Widget build (BuildContext context) => BottomAppBar(
		child: Center (child: Text ("This will be the bottom bar"))
	);
}